# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

## Frontend
node_modules

## Engine + export
dist
dist-ssr
*.local
engine/models/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local environment config (web share endpoint URLs etc.)
.env
.env.*
!.env.example

# Build/tooling output kept local, never committed
graphify-out/

# Local Tauri reference and its retired WebAssembly renderer
/legacy/
/tree-renderer/

# Generated review artifacts and nested Rust builds
/outputs/
**/target/
**/__pycache__/
*.pyc

# The engine uses the root workspace lockfile
/engine/Cargo.lock

# Local documentation and agent instructions
/docs/
AGENTS.md
CLAUDE.md
.agents/
.claude/

# Local migration experiments and visual comparison tools
/prototypes/
/tools/ui-parity/

# Generated translation indexes. tools/i18n_build.py rebuilds them from the
# data files on every run, so only the catalogues themselves are tracked.
data/i18n/sources.json
data/i18n/ui/sources.json
